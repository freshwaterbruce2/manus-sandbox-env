# Manus Sandbox Library Documentation

This document is automatically generated by `scripts/autodoc.py`. Do not edit manually.

## Module: `feature_flags.py`

**Path:** `tools/feature_flags.py`

```text
feature_flags.py — Lightweight Feature Flag utility for the Manus sandbox.

Provides a simple way to enable or disable features based on a configuration file.
This allows for safer deployments and experimental feature testing.
```

### Functions in `feature_flags.py`

#### `feature_flags.is_enabled()`

```text
Global helper to check if a feature is enabled.
```

### Classes in `feature_flags.py`

#### `class feature_flags.FeatureFlags`

```text
Manages feature flags using a JSON configuration file.
```

##### Methods

###### `FeatureFlags.__init__()`

```text
Initialize the feature flags manager.

Args:
    config_path: Path to the JSON configuration file.
```

###### `FeatureFlags._load_flags()`

```text
Load flags from the config file or return defaults.
```

###### `FeatureFlags.is_enabled()`

```text
Check if a feature is enabled.

Args:
    feature_name: The name of the feature to check.
    default: The default value if the feature is not found.

Returns:
    True if the feature is enabled, False otherwise.
```

###### `FeatureFlags.set_flag()`

```text
Set a feature flag and save to the config file.

Args:
    feature_name: The name of the feature.
    enabled: Whether the feature should be enabled.
```

## Module: `file_manager.py`

**Path:** `tools/file_manager.py`

```text
file_manager.py — Reusable file management utilities for the Manus sandbox.

Provides high-level functions for file operations like searching,
calculating hashes, and managing temporary directories.
```

### Functions in `file_manager.py`

#### `file_manager.get_file_hash()`

```text
Calculate the SHA-256 hash of a file.

Args:
    filepath: Path to the file.

Returns:
    The hex digest of the SHA-256 hash.
```

#### `file_manager.find_files_by_extension()`

```text
Find all files with a specific extension in a directory.

Args:
    directory: The root directory to search.
    extension: The file extension to look for (e.g., '.py').

Returns:
    A list of absolute file paths.
```

## Module: `github_helper.py`

**Path:** `tools/github_helper.py`

```text
github_helper.py — Reusable GitHub CLI wrapper for the Manus sandbox.

Provides convenience functions around common `gh` CLI operations so that
other scripts don't need to repeat subprocess boilerplate.
```

### Functions in `github_helper.py`

#### `github_helper._run()`

```text
Execute a shell command and return the CompletedProcess result.
```

#### `github_helper.get_authenticated_user()`

```text
Return the currently authenticated GitHub user as a dict.
```

#### `github_helper.list_repos()`

```text
List repositories for the authenticated user.

Args:
    limit: Maximum number of repos to return.
    visibility: One of 'all', 'public', 'private'.

Returns:
    List of repo dicts with keys: name, isPrivate, description, updatedAt.
```

#### `github_helper.create_issue()`

```text
Create a GitHub issue and return its URL.

Args:
    title: Issue title.
    body: Issue body (Markdown supported).
    repo: Optional repo in 'owner/name' format; defaults to current repo.

Returns:
    URL of the created issue.
```

#### `github_helper.create_pr()`

```text
Create a pull request from the current branch and return its URL.

Args:
    title: PR title.
    body: PR body (Markdown supported).
    base: Base branch to merge into.

Returns:
    URL of the created pull request.
```

#### `github_helper.get_repo_info()`

```text
Return metadata about a repository.

Args:
    repo: Optional repo in 'owner/name' format; defaults to current repo.

Returns:
    Dict with repo metadata.
```

## Module: `quality_gate.py`

**Path:** `scripts/quality_gate.py`

```text
quality_gate.py — The Manus Sandbox Quality Gate.

This script enforces strict code and documentation standards.
It runs:
1. Ruff (Python Formatting & Linting)
2. Mypy (Static Type Checking)
3. Markdownlint (Markdown Documentation Standards)

Any failure here prevents a commit or merge.
```

### Functions in `quality_gate.py`

#### `quality_gate.run_command()`

```text
Run a shell command and return (success, output).

Args:
    cmd: The shell command to execute.
    description: A human-readable description of the command.

Returns:
    A tuple of (success_boolean, combined_stdout_stderr).
```

#### `quality_gate.main()`

```text
Execute the quality gate checks for the entire repository.
```

## Module: `release.py`

**Path:** `scripts/release.py`

```text
release.py — Semantic Versioning and Git Tagging script.

This script manages the repository's versioning. It ensures that
releases are properly tagged in Git and follow SemVer (Major.Minor.Patch).
```

### Functions in `release.py`

#### `release.run_command()`

```text
Run a shell command and return the output.
```

#### `release.get_latest_tag()`

```text
Return the most recent Git tag, or None if no tags exist.
```

#### `release.increment_version()`

```text
Increment a semantic version string.

Args:
    version: The current version string (e.g., '1.0.0').
    part: The part to increment ('major', 'minor', or 'patch').

Returns:
    The incremented version string.
```

#### `release.main()`

```text
Run the release management process.
```

## Module: `sandbox_info.py`

**Path:** `scripts/sandbox_info.py`

```text
sandbox_info.py — Manus Sandbox Environment Inspector.

Prints a summary of the current sandbox state: OS, Python version,
installed packages, disk usage, and GitHub auth status.
```

### Functions in `sandbox_info.py`

#### `sandbox_info.run()`

```text
Run a shell command and return stdout.
```

#### `sandbox_info.section()`

```text
Print a section header with the given title.
```

#### `sandbox_info.main()`

```text
Run the sandbox information gathering and printing.
```
